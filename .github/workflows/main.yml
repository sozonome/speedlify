# .github/workflows/main.yml

name: Trigger Netlify Build daily on Weekday
on:
  schedule:
    # Every 20 minutes
    - cron: '0 22 * * MON-FRI'
jobs:
  build:
    name: Netlify build
    runs-on: ubuntu-latest
    steps:
      - name: Curl request
        run: curl -X POST -d {} https://api.netlify.com/build_hooks/5fa6c5fef8b3ba44837032b2